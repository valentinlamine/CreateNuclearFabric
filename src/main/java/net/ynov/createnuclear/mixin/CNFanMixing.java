package net.ynov.createnuclear.mixin;
/*
import com.chocohead.mm.api.ClassTinkerers;
import com.simibubi.create.AllRecipeTypes;
import com.simibubi.create.content.kinetics.fan.FanProcessing;
import com.simibubi.create.content.kinetics.fan.HauntingRecipe;
import io.github.fabricators_of_create.porting_lib.transfer.item.ItemStackHandlerContainer;
import net.minecraft.world.item.ItemStack;
import net.minecraft.world.level.Level;
import net.ynov.createnuclear.fan.CNFanProcessing;
import net.ynov.createnuclear.tools.EnrichedRecipe;
import org.spongepowered.asm.mixin.Mixin;

import java.util.Optional;

@Mixin(FanProcessing.class)
public class CNFanMixing implements Runnable{

    private static final CNFanProcessing.EnrichedWrapper ENRICHED_WRAPPER = new CNFanProcessing.EnrichedWrapper();

    private static boolean isEnriched(ItemStack stack, Level world) {
        ENRICHED_WRAPPER.setItem(0, stack);
        Optional<EnrichedRecipe> recipe = AllRecipeTypes.ENRICHED.find(ENRICHED_WRAPPER, world);
        return recipe.isPresent();
    }

    @Override
    public void run() {
        ClassTinkerers.enumBuilder("com.simibubi.create.content.kinetics.fan$Type")
                .build();
    }


}*/
